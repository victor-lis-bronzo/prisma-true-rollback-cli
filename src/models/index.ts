/**
 * Barrel re-export for the shared data models.
 *
 * Only `./types` is re-exported here. The typed error classes live in
 * `./errors` (Task 2.2), which adds its own re-export.
 */
export * from './types.js';
